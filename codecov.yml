codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
    patch:
      default:
        target: 100%

comment:
  layout: "reach,diff,flags,tree"
  behavior: default